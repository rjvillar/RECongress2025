#!/usr/bin/env node

/**
 * This script is intentionally left empty.
 *
 * The deployment pipeline moved to a PHP-based asset proxy, so no
 * post-export filesystem rewrites are required anymore. The file remains
 * as a placeholder to avoid breaking older documentation.
 */
